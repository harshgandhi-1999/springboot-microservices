spring:
  application:
    name: "configserver"
  profiles:
    #active: native
    active: git
  cloud:
    config:
      server:
       #native:
        #search-locations: "classpath:/config"
        #search-locations: "file:///E://spring-projects//eazybytes//documents//config"
        git:
          uri: "https://github.com/harshgandhi-1999/eazybytes-config.git"
          default-label: main
          timeout: 5   # 5 second
          clone-on-start: true
          force-pull: true
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"

encrypt:
  key: "gTvQjuzulXAmYLQxN1cf7fYcwGnDlnUn"

server:
  port: 8071


# we should use hyphens instead of underscore in the file name to cloud config to work